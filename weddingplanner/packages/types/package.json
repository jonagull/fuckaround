{
  "name": "weddingplanner-types",
  "publishConfig": {
    "access": "public"
  },
  "version": "1.0.17",
  "description": "Shared TypeScript types for wedding planner application",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "pub": "pnpm version patch --no-git-tag-version && pnpm run build && pnpm publish --no-git-checks"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
