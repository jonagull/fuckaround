export enum EventRole {
    Owner,
    Admin,
    Editor,
    Reader,
}

export enum InvitationStatus {
    Pending,
    Accepted,
    Rejected,
}


export enum EventType {
    Wedding,
    Birthday,
    Other,
}